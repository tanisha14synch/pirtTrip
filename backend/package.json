{
  "name": "pirt-trip-backend",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bash -lc 'set -a && [ -f .env ] && source .env; set +a; nitro dev --port 3001 --host 127.0.0.1'",
    "build": "nitro build",
    "start": "node .output/server/index.mjs",
    "preview": "node .output/server/index.mjs",
    "db:migrate": "node scripts/run-supabase-migrations.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "nitropack": "^2.13.4",
    "nodemailer": "^8.0.10",
    "pg": "^8.21.0",
    "resend": "^6.12.4",
    "ws": "^8.21.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/nodemailer": "^8.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.8.3"
  }
}
