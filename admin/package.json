{
  "name": "pirt-trip-admin",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "bash -lc 'lsof -ti:3002 | xargs kill -9 2>/dev/null || true; TMPDIR=/tmp nuxt dev --port 3002'",
    "dev:clean": "bash -lc 'lsof -ti:3002 | xargs kill -9 2>/dev/null || true; rm -rf .nuxt node_modules/.cache && TMPDIR=/tmp nuxt dev --port 3002'",
    "preview": "nuxt preview",
    "start": "node .output/server/index.mjs",
    "postinstall": "nuxt prepare"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "nuxt": "^3.16.2",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "typescript": "^5.8.3"
  }
}
